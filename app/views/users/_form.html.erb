<%= form_for @user do |u| %>
    <%= u.label :username %>
    <%= u.text_field :username, required: true %><br>

    <%= u.label :email%>
    <%= u.email_field :email, required: true %><br>

    <%= u.label :password%>
    <%= u.password_field :password, required: true %><br>

    <%= u.label :password %>
    <%= u.password_field :password_confirmation %><br>

    <%= u.submit "Create New Account!"%>
<% end %>